Options -Indexes
RewriteEngine On

# Change the rewritebase to your base
RewriteBase /~ruudgreven/roosterious/web
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ site.php [QSA,L]
